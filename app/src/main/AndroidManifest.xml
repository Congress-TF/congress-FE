<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".presentation.util.ApplicationClass"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Congress"
        tools:targetApi="34">

        <activity
            android:name=".presentation.ui.login.LoginActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.ui.login.SignInActivity"
            android:exported="true">

        </activity>
        <activity
            android:name=".presentation.ui.main.MainActivity"
            android:exported="true">

        </activity>

         <activity
            android:name=".presentation.ui.act.ActActivity"
            android:exported="true">

        </activity>

        <activity
            android:name=".presentation.ui.revision.RevisionActivity"
            android:exported="true">

        </activity>

        <activity
            android:name=".presentation.ui.home.HomeActivity"
            android:exported="true" >

        </activity>

        <activity
            android:name=".presentation.ui.mypage.info.MyInfoActivity"
            android:exported="true">


        </activity>

        <activity
            android:name=".presentation.ui.mypage.myAct.MyActActivity"
            android:exported="true" >
        </activity>

        <activity
            android:name=".presentation.ui.mypage.myLegis.MyLegisLegislatorActivity"
            android:exported="true" >
        </activity>

    </application>

</manifest>